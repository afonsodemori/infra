services:
  certbot:
    image: certbot/dns-cloudflare
    environment:
      EMAIL_ADDRESS: ${CERTBOT_EMAIL_ADDRESS:?}
      CLOUDFLARE_API_TOKEN: ${CERTBOT_CLOUDFLARE_API_TOKEN:?}
      PROPAGATION_SECONDS: ${CERTBOT_PROPAGATION_SECONDS:?}
    volumes:
      - ${DATA_DIR:?}/letsencrypt/data:/etc/letsencrypt
      - ${DATA_DIR:?}/letsencrypt/logs:/var/log/letsencrypt
      - ${INFRA_DIR:?}/docker/compose/certbot/entrypoint.sh:/entrypoint.sh:ro
    entrypoint: ["/bin/sh", "/entrypoint.sh"]
    profiles:
      - ondemand
    restart: never
