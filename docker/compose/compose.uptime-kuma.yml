services:
  uptime-kuma:
    image: louislam/uptime-kuma:${KUMA_TAG:-latest}
    volumes:
      - ${DATA_DIR:?}/uptime-kuma:/app/data
      - ${INFRA_DIR:?}/docker/compose/uptime-kuma/health.json:/app/dist/health.json:ro
    environment:
      - TZ=UTC
      - UMASK=0022
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    restart: unless-stopped
