services:
  afonsodev-production-api:
    image: ghcr.io/afonsodemori/afonsodev-api:${API_TAG:-latest}
    env_file:
      - .env
    restart: unless-stopped

  afonsodev-production-web:
    image: ghcr.io/afonsodemori/afonsodev-web:${WEB_TAG:-latest}
    restart: unless-stopped
